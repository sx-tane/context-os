[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "context-os-ai-worker"
version = "0.1.0"
description = "ContextOS AI worker scaffold"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.setuptools]
py-modules = ["embed", "health"]
