# Issue 10: Build normalization pipeline

## Description

Convert raw ingestion events into normalized documents with stable metadata.

## Acceptance criteria

- Pipeline accepts raw ingestion events.
- Output documents include ID, source, type, title, body, metadata, and normalization timestamp.
