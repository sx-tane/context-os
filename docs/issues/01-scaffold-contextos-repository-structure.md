# Issue 1: Scaffold ContextOS repository structure

## Description

Create the planned ContextOS folder layout from `MASTER_PLAN.md` so future prompts can target one domain at a time.

## Acceptance criteria

- `apps/`, `internal/`, `shared/`, `storage/`, `docs/`, `scripts/`, `migrations/`, `prompts/`, `docker/`, and `tests/` exist.
- Domain folders exist under `internal/`.
- Shared contract, event, pipeline, entity, and type folders exist under `shared/`.
