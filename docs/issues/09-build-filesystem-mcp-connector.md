# Issue 9: Build filesystem MCP connector

## Description

Implement local file ingestion through MCP.

## Acceptance criteria

- Filesystem connector implements the shared MCP connector contract.
- Connector declares file capability.
- Connector produces raw ingestion events.
